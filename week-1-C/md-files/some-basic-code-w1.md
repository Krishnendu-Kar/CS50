What is source code?
=Source code is the human-readable set of instructions, written in a programming language, that programmers create to tell a computer what to do. It's typically compiled or interpreted into machine code (or bytecode) that the computer can actually execute.

visit manual.cs50.io to get the special library function work