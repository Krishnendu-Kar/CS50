in c in for loop
 for(i=6;i>=1;i--){
        for(c=i;c>=1;c--){
            printf("#");
        }
        printf("\n");
    }

 here at first initialization happen, then condition is checked, after that work is done inside { } then update variable, then condition check then work is done inside { } then update variable, like this again and again until condition become false.